# 三个后续方向：验证记录

状态：执行中；首轮固定激活 BN 诊断出现层间激活放大，正在进行一次校准方式修正，融合与蒸馏仍在训练。此文件对应本会话约定的归一化、静态/动态延迟融合、同缺失输入专家蒸馏。

## 固定实验与判定

- 原始 T1 Res18-U-Net / T5 Res18-UTAE 初始化，训练年 2016–2020，2021 seed 0 完整筛选；每个场景 3181 个样本。
- 学习实验使用相同 AdamW、初始 LR .001、cosine 衰减、3000 更新、物理与有效 batch 64。控制、混合浅层、分组浅层、蒸馏四臂均重新运行匹配对照。
- 归一化：4096 个训练样本，冻结 X22 权重；同一批固定前向激活估计公共统计与块缺失有/无两套统计。两者均不更新梯度、Dropout 或原始 BN 缓冲。先比较公共、条件和原始三种结果；同时超过公共统计和原始 X22，且通过 clean guardrail，才进入独立 BN affine 参数的后续实验。
- 融合：两层 3x3 非线性残差浅层在原主干混合通道前处理输入；typed 分开静态和动态，mixed 混合处理。初始化均为恒等映射，两臂新增参数量完全相同：T1 为 11,520，T5 为 9,504。typed 相对 mixed 的缺失主指标增益在两种设置中均须至少 +.005，并超过原 cosine 控制；M00 相对两种控制均不得下降超过 .010。
- 蒸馏：师生使用同一份缺失后输入；教师按可观测掩码固定选择 ERM / X22 / 25% / 50% 专家，块缺失优先。原预测损失加 .1 Bernoulli KL，全学生训练。两种设置均须超过无蒸馏学生，并距教师主指标不超过 .003；M00 不得比控制下降超过 .010。关注权重存储，不能预先声称推理更快。
- 正向正式筛选需补 seeds 1/2 和冻结的 2022/2023 报告；未通过则停止该配方，不做参数搜索。历史测试年份已经暴露，不能称为全新未见测试。

## 已完成的执行检查

| 检查 | 证据 |
|---|---|
| 既有评价与架构检查 | CPU Slurm 21786796，20 tests passed |
| 小模块单元测试 | CPU Slurm 21787126，10 tests passed |
| 驱动和两套 BN 路由检查 | CPU Slurm 21787125，4 tests passed |
| 结果判定检查 | 3 个纯 JSON/数值汇总测试通过 |
| T1 五臂真实数据 smoke | GPU Slurm 21786978，全部通过 |
| T5 五臂真实数据 smoke | GPU Slurm 21786979，全部通过 |
| 初始化与 checkpoint | control/mixed/typed 初始损失相同；所有重载预测最大差为 0 |

归一化收集器已根据代码审查改为 GPU 汇总、只向 CPU 传逐通道统计量；扩展 smoke 21787156/21787157 已通过 M00/M01/M06/M07 的公共和条件分支、两套掩码路由及重载一致性检查。修订只影响 BN 诊断，不改变正在运行的学习实验。

## 正式任务

| 设置 | cosine 控制 | mixed 浅层 | typed 浅层 | 专家蒸馏 |
|---|---:|---:|---:|---:|
| T1 | 21787115 | 21787116 | 21787117 | 21787118 |
| T5 | 21787119 | 21787120 | 21787121 | 21787122 |

完整提交指令及不可变 commit 见 `three_directions_jobs.json`；教师路径、元数据和 SHA256 见 `three_directions_manifest.json`。运行产物位于 `/project/6085198/kulbear/wildfire/runs/three-directions-*`。训练任务从 commit `e7db913` 归档执行；研究分支是 `research/reliability-fusion-20260912`。

## 待完成

- [ ] 修订 BN smoke 与两个设置的完整校准/评价。
- [ ] 八个学习任务的完整 3000 更新及四场景评价。
- [ ] 按预定配对门槛判定三个方向。
- [ ] 对通过门槛的方向完成后续确认；报告未通过的方向及范围限制。
- [ ] 汇总 AP、逐场景差值、计算与存储成本，最终报告并提交。

BN 正式任务：T1 21787290, T5 21787291。

## 固定激活 BN 诊断与校准修正

旧激活下估计、同时替换全部 BN 统计的诊断结果（完整 2021，seed 0）：

| 设置 | 条件统计相对原 X22 主指标差 | 条件统计相对公共统计差 | M00 相对原 X22 差 |
|---|---:|---:|---:|
| T1 | -0.260871 | -0.244285 | -0.057871 |
| T5 | -0.096705 | -0.096251 | -0.004196 |

CPU 统计审计 21787565 确认：两套统计的样本数合计4096，方差均非零，合并后可在浮点误差内还原公共统计。GPU 激活审计21787609确认：同一批M06输入直接应用统计与通过路由应用，预测差为0；T1却在层间逐渐放大，最终BN输出RMS约1.99e6。对应代码是 `audit_bn_statistics.py` 和 `audit_bn_activations.py`。

因此保留上述数字作为固定激活直接替换的负结果，并追加一次 batch-stat 校准：只让 BN 在收集时按当前 batch 标准化，Dropout关闭，不改变权重或BN缓冲，其他条件全部固定。其结果与旧诊断分开存储；不能用旧诊断直接否定整个条件归一化方向。原始记录见 `three_directions_fixed_bn_diagnostic.json`。

## 已完成的 T1 学习筛选（2021，seed 0）

| 模型 | M00 | M01 | M06 | M07 | 缺失主指标 |
|---|---:|---:|---:|---:|---:|
| control | 0.594210 | 0.320054 | 0.379640 | 0.197741 | 0.299145 |
| mixed | 0.600058 | 0.324620 | 0.385067 | 0.199692 | 0.303126 |
| typed | 0.593895 | 0.318619 | 0.375692 | 0.190141 | 0.294817 |
| distill | 0.595446 | 0.321369 | 0.383130 | 0.202723 | 0.302407 |

T1 typed 相对等容量 mixed 主指标 −0.008309，相对 cosine control −0.004328；未通过预定门槛。对应三个任务均完成3000更新并退出0:0；T5仍按原协议完成，不提前取消。此处是单种子验证集筛选，不是跨年份结论。

修订后的 batch-stat BN 全场景 smoke 21788071/21788072 均通过，两套统计和重载预测差为0。完整4096训练样本校准及2021评价已提交为21788328/21788332，源commit为7a58b38。

T1蒸馏相对无KD控制的主指标为+0.003262，但相对固定教师组合为−0.003851，低于预定−0.003容差，因此未通过压缩筛选；M00相对无KD为+0.001236。完整逐场景差值见 `three_directions_t1_partial.json`。这里的方向状态仍为incomplete，直到T5对照和候选全部完成。

修订BN校准的独立代码审查已完成：未发现阻断问题；只启用BN的batch统计、Dropout关闭、共同/条件池共享完全相同的激活汇总、完整state_dict未改动、恢复标志与模式元数据均已检查。结论限定于固定物理batch和同类缺失分组的校准方式，旧fixed诊断单独披露。
